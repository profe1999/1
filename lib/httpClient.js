/**
 * lib/httpClient.js — fetch с явным доверием к системным сертификатам.
 * Оставлено на случай, если какой-то из источников (в будущем) снова
 * потребует нестандартную цепочку сертификатов — сейчас (MOEX ISS,
 * smart-lab.ru) в этом обычно нет необходимости, но безопаснее иметь один
 * общий fetch, который легко расширить, чем гадать на будущее.
 */
const { fetch } = require("undici");
module.exports = { fetch };
